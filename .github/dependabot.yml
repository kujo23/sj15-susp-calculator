version: 2
updates:
  # Keeps GitHub Actions versions (checkout, setup-node) up to date.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
      day: monday
    commit-message:
      prefix: "chore"
